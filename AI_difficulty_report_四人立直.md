# 四人立直麻将 AI 难度审查报告

## 结论
- 当前 AI 属于“简单”级（略强于随机出牌、尚未达到中等）：有基础牌效和役牌鸣牌意识，但没有打点规划、防守、局况判断或稳定的策略随机性。

## 1. 决策入口与调用链
- `App.tsx` 的定时 effect 每 300ms 检查 AI 回合或无人类可处理的鸣牌窗口，并调用 `advanceAIAction(GameState)`。
- `advanceAIAction` 是总入口：摸牌调用 `drawTile`；弃牌阶段依次处理九种九牌、立直、暗杠/加杠、选牌与 `discardTile`；鸣牌窗口按大明杠→碰→吃→跳过处理。
- 自摸由 `drawTile` / 杠后 `applyKanDraw` 调用正式和牌结算自动完成；普通荣和由 `discardTile` 中的 `buildRonResult` 自动完成，抢杠荣由 AI 显式调用 `declareChankanRon`。

## 2. 出牌指标
- `selectAIDiscardTile` 调用 `recommendDiscards`，排序顺序是：打后向听数更低→可见剩余有效牌总数更多→优先切幺九字牌→尽量不破坏对子及一二间隔搭子。
- 可见牌计数包含自己的手牌、四家牌河、公开副露和宝牌指示牌；赤五按基础牌 ID 合并。
- 已鸣弃牌同时存在于牌河和副露时会被重复计数，可能低估部分有效牌剩余量。

## 3. 向听、有效牌、打点、危险度与随机性
- 使用标准形、七对子、国士的综合向听数，并使用有效牌种类及可见剩余张数。
- 不计算预期打点、役种路线、宝牌价值、赤牌保留、庄闲/点差/局次，也不比较立直与默听收益。
- 不计算铳率、现物、筋、壁、对手立直或副露威胁，完全没有防守逻辑。
- `rng` 只在推荐结果不存在时随机选手牌；正常非空手牌几乎总有推荐，因此实际决策基本确定性，随机性很弱。

## 4. 吃碰杠与立直条件
- 吃：仅选择使“考虑现有副露后的标准形向听数”下降的第一个合法方案。
- 碰：役牌必碰；非役牌仅在移除两张后基础向听数下降时碰，未评估开门后是否有役或打点损失。
- 大明杠：役牌必杠，其他牌仅在当前手牌已听牌或和牌时杠；暗杠/加杠也仅在向听数≤0时执行，并服从引擎的立直后保留等待限制。
- 立直：只要引擎判定门清、点数足够且存在打后听牌候选，就无条件立直；随后弃牌仍走通用推荐，未显式限制为 `getRiichiDiscardCandidates`，存在立直后选到非听牌弃牌的结构性风险。
- 荣和、自摸、抢杠荣均在合法且有役时必和，不做见逃或点况选择；九种九牌满足条件时必流局。

## 5. 信息边界与多 AI 一致性
- 当前策略代码没有读取对手暗手、未公开牌山、王牌或里宝牌；正式和牌校验虽使用完整状态，但不把隐藏信息用于策略评分。
- 风险在接口层：`advanceAIAction` 直接接收含全部隐藏数据的 `GameState`，未来策略或模型若直接使用它，容易发生信息泄漏。
- Player 2、3、4 都由 `playerId !== 0` 判为 AI，共用同一个入口、指标、阈值和默认 `Math.random`，没有座位专属或难度差异。

## 6. 四档难度与强化学习接入建议
- 保留规则引擎的合法动作接口：`can/get*Candidates`、`canTsumo/canRon`、`getRiichiDiscardCandidates`、`execute/discard/declare*`，策略只选动作，不自行修改状态。
- 保留 `advanceAIAction(state, rng)` 作为执行编排边界、`selectAIDiscardTile` 作为弃牌策略边界，以及可注入 `rng` 的可复现测试能力。
- 新增只含本人手牌和公开信息的 `AIObservation`、统一 `LegalAction[]`、`AIPolicy.chooseAction(observation, legalActions, rng)`；四档难度仅替换 policy。
- 强化学习保留稳定动作 ID、观测编码、奖励/终局结果、随机种子和动作日志；禁止模型直接读取完整 `GameState`，并由引擎再次校验所有输出动作。

本次仅静态审查现有实现，未修改源码，未运行测试。
